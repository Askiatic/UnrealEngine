#include <bits/stdc++.h>

#define int long long

inline void run() {
    
}

signed main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);

    int T = 1;
    std::cin >> T;
    while (T--) run();

    return 0;
}